-- Schema operativo (citas, clínicas, doctoras, pacientes, pagos).
-- Se definirá cuando se reciba el CSV con datos ficticios.
-- Placeholder para que sqlc no falle al generar.

CREATE TABLE _placeholder (
    id INT PRIMARY KEY
);
